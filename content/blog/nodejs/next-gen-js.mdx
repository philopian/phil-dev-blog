---
title: ES-Next
date: "2021-02-02"
description: ES-Next
---



# Primitive Variables 
- [Primitive](https://developer.mozilla.org/en-US/docs/Glossary/Primitive)
- There are 7 primitive data types: `string`, `number`, `bigint`, `boolean`, `undefined`, `symbol`, and `null`.
- All primitives are **immutable**, they cannot be altered
- The variable may be reassigned a new value, but the existing value can not be changed in the ways that objects, arrays, and functions can be altered.

# String helper functions

# `let` & `const`
- `var` still works but it's encouraged to you`let` & `const`


# Strings
- [Strings](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String)
- Static Methods
  ```js
  String.fromCharCode(189) // "½"
  String.fromCodePoint(9731) // "☃"
  String.raw(`cat`)
  ```
- Instance Properties
  ```js
  'cat'.length // 3
  ```
- Instance Methods
  ```js
  'some string'.concat() 
  'some string'.includes()
  'some string'.endsWith()
  'some string'.indexOf()
  'some string'.lastIndexOf()
  'some string'.match()
  'some string'.repeat()
  'some string'.replace()
  'some string'.replaceAll()
  'some string'.search(regex)
  'some string'.slice(beginIndex, endIndex)
  'some string'.split() // returns an array
  'some string'.startsWith()
  'some string'.substr()
  'some string'.toLowerCase()
  'some string'.toString()
  'some string'.toUpperCase()
  'some string'.trim()
  'some string'.trimStart()
  'some string'.trimEnd()
  'some string'.valueOf()
  ```

# Number


# Array




# Objects



# Standard built-in objects
- [](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects)
- Value Properties
  - These global properties return a simple value. They have no properties or methods.
    ```js
    Infinity
    NaN
    undefined
    globalThis
    ```
- Function Properties
  - These global functions—functions which are called globally, rather than on an object—directly return their results to the caller.
    ```js
    eval()
    isFinite()
    isNaN()
    parseFloat()
    parseInt()
    encodeURI()
    encodeURIComponent()
    decodeURI()
    decodeURIComponent()
    ```



# Arrow Function





- arrow functions
- default exports (modules)
- named exports (modules)












